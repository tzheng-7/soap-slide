start_x = x;
start_y = y;

vspd = -2.5; // Starting upward speed (negative = up)
speed_increment = -0.5; // How much faster every time
alarm[0] = 3 * 60; // 5 seconds in terms of frames (60 FPS = 5 seconds)